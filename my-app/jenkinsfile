pipeline{
  agent any
  environment{
    PATH = /usr/bin:$PATH
  }
  stages{
      stage('build'){
        steps{
          echo "building an appliaction"
          sh "mvn clean install"
        }
      }
    }
}

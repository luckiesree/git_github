node {
  stage('SCM checkout'){
    git "https://github.com/luckiesree/git_github.git"
    }
  stage('maven build'){
    git "https://github.com/luckiesree/git_github.git"
    def mvnHome = /usr/share/maven
    sh "${mvnHome}/bin/mvn package"
  }
}
